Keeping track of dependencies here.

1. at - non-recurring task scheduler
> sudo apt install at

2. SOund eXpress - command line audio editor, recorder & player
> sudo apt install sox

3. mp3 handler for sox
> sudo apt install libsox-fmt-mp3

4. zenity - GTK3 dialog creator
> sudo apt install zenity
